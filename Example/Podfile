use_frameworks!

platform :ios, '11.0'

target 'RyTransitioningDelegate_Example' do
  pod 'RyTransitioningDelegate', :path => '../'

  target 'RyTransitioningDelegate_Tests' do
    inherit! :search_paths

    
  end
end
